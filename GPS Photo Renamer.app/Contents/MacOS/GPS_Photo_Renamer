#!/bin/bash
export PATH="/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:$PATH"

# GPS Photo Renamer - UI Version v1.0.0
# Renames photos based on GPS EXIF data with native macOS dialogs
# v2.1.0: Video detection and deletion option
# v2.2.0: Map tile watermark from OpenStreetMap
# v2.3.0: Map option dialog, reprocess existing photos, _MAP tag, bigger map
# v2.4.0: Custom map settings (size, transparency, zoom)
# v2.4.1: Fixed reprocess-map mode, improved location extraction
# v2.5.0: Forward geocoding for reprocess (when EXIF GPS lost), preserve EXIF on save
# v2.5.1: Fix EXIF orientation issue (images rotated twice) using piexif

# Funktion für Benachrichtigungen
notify() {
    osascript -e "display notification \"$2\" with title \"GPS Photo Renamer\" subtitle \"$1\""
}

# Funktion für Dialogs
show_dialog() {
    osascript -e "display dialog \"$1\" with title \"GPS Photo Renamer\" buttons {\"OK\"} default button 1"
}

# Funktion für Fehler-Dialogs
show_error() {
    osascript -e "display dialog \"❌ Fehler: $1\" with title \"GPS Photo Renamer\" buttons {\"OK\"} default button 1 with icon stop"
}

# Logging
LOG_FILE="$HOME/Desktop/gps_photo_renamer.log"
exec > "$LOG_FILE" 2>&1

echo "=== GPS Photo Renamer v2.5.1 ==="
echo "$(date)"
echo ""

# Schritt 1: Prüfe und installiere Dependencies
notify "Prüfe System" "Überprüfe installierte Tools..."

INSTALL_FAILED=0

# Prüfe Python
if ! command -v python3 &> /dev/null; then
    show_error "Python 3 nicht gefunden!\n\nBitte installiere Python 3:\nbrew install python3"
    exit 1
fi

# Prüfe Pillow
if ! python3 -c "from PIL import Image" 2>/dev/null; then
    notify "Installation" "Installiere Pillow..."

    if ! pip3 install Pillow --break-system-packages 2>&1; then
        show_error "Pillow Installation fehlgeschlagen!\n\nBitte installiere manuell:\npip3 install Pillow --break-system-packages"
        INSTALL_FAILED=1
    fi
fi

# Prüfe requests
if ! python3 -c "import requests" 2>/dev/null; then
    notify "Installation" "Installiere requests..."

    if ! pip3 install requests --break-system-packages 2>&1; then
        show_error "requests Installation fehlgeschlagen!\n\nBitte installiere manuell:\npip3 install requests --break-system-packages"
        INSTALL_FAILED=1
    fi
fi

# Prüfe piexif (für EXIF-Orientierung)
if ! python3 -c "import piexif" 2>/dev/null; then
    notify "Installation" "Installiere piexif..."

    if ! pip3 install piexif --break-system-packages 2>&1; then
        echo "⚠️ piexif Installation fehlgeschlagen (optional)"
        # Nicht kritisch - App funktioniert auch ohne
    fi
fi

if [ $INSTALL_FAILED -eq 1 ]; then
    osascript <<'HELP_DIALOG'
display dialog "⚠️ Installation fehlgeschlagen!

📋 Bitte führe die folgenden Schritte MANUELL aus:

1️⃣ Python 3 installieren (falls nicht vorhanden):
   brew install python3

2️⃣ Python-Pakete installieren:
   pip3 install Pillow requests piexif --break-system-packages

📄 Details im Log: ~/Desktop/gps_photo_renamer.log

Nach der manuellen Installation die App erneut starten!" buttons {"OK"} default button 1 with title "GPS Photo Renamer - Installation" with icon stop
HELP_DIALOG
    exit 1
fi

echo "✓ Alle Dependencies installiert"
notify "Bereit!" "Alle Tools sind installiert"
echo ""

# Schritt 2: Wähle Foto-Ordner
folder_path=$(osascript <<'APPLESCRIPT' 2>/dev/null
try
    set theFolder to choose folder with prompt "Wähle einen Ordner mit Fotos:"
    return POSIX path of theFolder
on error
    return "ABGEBROCHEN"
end try
APPLESCRIPT
)

if [[ "$folder_path" == "ABGEBROCHEN" ]] || [[ -z "$folder_path" ]]; then
    exit 0
fi

# Entferne trailing slash
folder_path="${folder_path%/}"

echo "Gewählter Ordner: $folder_path"

# Zähle Fotos im Ordner
photo_count=$(find "$folder_path" -maxdepth 1 -type f \( -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" -o -iname "*.heic" -o -iname "*.heif" \) ! -name "._*" | wc -l | tr -d ' ')

if [ "$photo_count" -eq 0 ]; then
    show_error "Keine Fotos gefunden!\n\nUnterstützte Formate:\n• JPG/JPEG\n• PNG\n• HEIC/HEIF"
    exit 1
fi

echo "Gefundene Fotos: $photo_count"

# Schritt 3: Zeige Optionen-Dialog
options_result=$(osascript <<APPLESCRIPT 2>/dev/null
try
    set theChoice to display dialog "📸 GPS Photo Renamer

📁 Ordner: $(basename "$folder_path")
📷 Fotos gefunden: $photo_count

Wähle die gewünschten Optionen:" buttons {"Abbrechen", "Optionen wählen"} default button 2 with title "GPS Photo Renamer"

    if button returned of theChoice is "Abbrechen" then
        return "ABGEBROCHEN"
    end if

    -- Log-Modus Option
    set dryrunChoice to button returned of (display dialog "📋 Erst nur protokollieren (Log-Modus)?

Im Log-Modus werden keine Dateien geändert.
Du siehst zuerst, was passieren würde.

Nach dem Log kannst du direkt umbenennen." buttons {"Nein, direkt umbenennen", "Ja, erst Log anzeigen"} default button 2 with title "GPS Photo Renamer - Modus")

    -- Karten-Option
    set mapChoice to button returned of (display dialog "🗺️ Kartenausschnitt hinzufügen?

Ein kleiner OpenStreetMap-Ausschnitt wird
rechts oben auf dem Foto angezeigt." buttons {"Nein", "Ja"} default button 2 with title "GPS Photo Renamer - Karte")

    -- Karten-Einstellungen (nur wenn Karte gewählt)
    set mapSettingsChoice to "Standard"
    set mapSizeChoice to "Mittel"
    set mapTransparencyChoice to "Normal"
    set mapZoomChoice to "Stadtteil"

    if mapChoice is "Ja" then
        -- Standard oder Benutzerdefiniert?
        set mapSettingsChoice to button returned of (display dialog "⚙️ Karten-Einstellungen

Standard: 15% vom Bild, 70% sichtbar, Stadtteil-Zoom
Benutzerdefiniert: Größe, Transparenz und Zoom selbst wählen" buttons {"Standard", "Benutzerdefiniert"} default button 1 with title "GPS Photo Renamer - Karten-Einstellungen")

        if mapSettingsChoice is "Benutzerdefiniert" then
            -- Größe wählen
            set mapSizeChoice to button returned of (display dialog "📐 Kartengröße wählen

Klein: 10% vom Bild
→ Dezent, wenig Platz

Mittel: 15% vom Bild
→ Gut sichtbar, Standard

Groß: 20% vom Bild
→ Sehr prominent, mehr Details" buttons {"Klein", "Mittel", "Groß"} default button 2 with title "GPS Photo Renamer - Kartengröße")

            -- Transparenz wählen
            set mapTransparencyChoice to button returned of (display dialog "👁️ Karten-Transparenz wählen

Dezent: 50% sichtbar
→ Foto scheint stark durch

Normal: 70% sichtbar
→ Gute Balance, Standard

Kräftig: 90% sichtbar
→ Karte sehr deutlich" buttons {"Dezent", "Normal", "Kräftig"} default button 2 with title "GPS Photo Renamer - Transparenz")

            -- Zoom wählen
            set mapZoomChoice to button returned of (display dialog "🔍 Karten-Zoom wählen

Straße: Zoom 15
→ Zeigt Straßen, genaue Position

Stadtteil: Zoom 13
→ Zeigt Nachbarschaft/Dorf, Standard

Region: Zoom 11
→ Zeigt größere Umgebung/Stadt" buttons {"Straße", "Stadtteil", "Region"} default button 2 with title "GPS Photo Renamer - Zoom")
        end if
    end if

    -- Wenn Karte gewünscht: Frage nach bereits bearbeiteten Fotos
    set reprocessMapChoice to "Nein"
    if mapChoice is "Ja" then
        set reprocessMapChoice to button returned of (display dialog "🔄 Bereits bearbeitete Fotos nochmal mit Karte versehen?

Fotos die schon umbenannt wurden (aber noch keine Karte haben) können nachträglich eine Karte bekommen." buttons {"Nein, nur neue", "Ja, alle mit Karte"} default button 1 with title "GPS Photo Renamer - Karte nachträglich")
    end if

    -- Wasserzeichen und Geocoding immer aktiv, Unterordner nie
    set watermarkChoice to "Ja"
    set geocodingChoice to "Ja"
    set recursiveChoice to "Nein"

    return watermarkChoice & "|" & geocodingChoice & "|" & recursiveChoice & "|" & dryrunChoice & "|" & mapChoice & "|" & reprocessMapChoice & "|" & mapSizeChoice & "|" & mapTransparencyChoice & "|" & mapZoomChoice

on error
    return "ABGEBROCHEN"
end try
APPLESCRIPT
)

echo "Optionen-Ergebnis: $options_result"

if [[ "$options_result" == "ABGEBROCHEN" ]] || [[ -z "$options_result" ]]; then
    exit 0
fi

# Parse Optionen
IFS='|' read -r WATERMARK GEOCODING RECURSIVE DRYRUN MAP_OPTION REPROCESS_MAP MAP_SIZE MAP_TRANSPARENCY MAP_ZOOM <<< "$options_result"

echo "Watermark: $WATERMARK"
echo "Geocoding: $GEOCODING"
echo "Recursive: $RECURSIVE"
echo "Dry-Run: $DRYRUN"
echo "Map: $MAP_OPTION"
echo "Reprocess Map: $REPROCESS_MAP"
echo "Map Size: $MAP_SIZE"
echo "Map Transparency: $MAP_TRANSPARENCY"
echo "Map Zoom: $MAP_ZOOM"

# Baue Python-Argumente
PYTHON_ARGS=""

if [[ "$WATERMARK" == "Ja" ]]; then
    PYTHON_ARGS="$PYTHON_ARGS --watermark"
fi

if [[ "$GEOCODING" == "Nein" ]]; then
    PYTHON_ARGS="$PYTHON_ARGS --no-geocoding"
fi

if [[ "$RECURSIVE" == "Ja" ]]; then
    PYTHON_ARGS="$PYTHON_ARGS --recursive"
fi

if [[ "$MAP_OPTION" == "Ja" ]]; then
    PYTHON_ARGS="$PYTHON_ARGS --map"

    # Map Size (in Prozent vom Bild) - Standard = 15%
    case "$MAP_SIZE" in
        "Klein")
            PYTHON_ARGS="$PYTHON_ARGS --map-size 10"
            ;;
        "Groß")
            PYTHON_ARGS="$PYTHON_ARGS --map-size 20"
            ;;
        *)
            # Standard/Mittel oder leer = 15%
            PYTHON_ARGS="$PYTHON_ARGS --map-size 15"
            ;;
    esac

    # Map Transparency - Standard = 70%
    case "$MAP_TRANSPARENCY" in
        "Dezent")
            PYTHON_ARGS="$PYTHON_ARGS --map-opacity 50"
            ;;
        "Kräftig")
            PYTHON_ARGS="$PYTHON_ARGS --map-opacity 90"
            ;;
        *)
            # Standard/Normal oder leer = 70%
            PYTHON_ARGS="$PYTHON_ARGS --map-opacity 70"
            ;;
    esac

    # Map Zoom - Standard = 13 (Stadtteil)
    case "$MAP_ZOOM" in
        "Straße")
            PYTHON_ARGS="$PYTHON_ARGS --map-zoom 15"
            ;;
        "Region")
            PYTHON_ARGS="$PYTHON_ARGS --map-zoom 11"
            ;;
        *)
            # Standard/Stadtteil oder leer = 13
            PYTHON_ARGS="$PYTHON_ARGS --map-zoom 13"
            ;;
    esac
fi

if [[ "$REPROCESS_MAP" == "Ja, alle mit Karte" ]]; then
    PYTHON_ARGS="$PYTHON_ARGS --reprocess-map"
fi

LOG_MODE="false"
if [[ "$DRYRUN" == "Ja, erst Log anzeigen" ]] || [[ "$DRYRUN" == "Ja, nur protokollieren" ]]; then
    PYTHON_ARGS="$PYTHON_ARGS --dry-run"
    LOG_MODE="true"
fi

echo "Python-Argumente: $PYTHON_ARGS"

# Schritt 4: Bestätigung
confirm_result=$(osascript <<APPLESCRIPT 2>/dev/null
try
    set optionList to ""

    -- Immer aktiv
    set optionList to optionList & "• Wasserzeichen: Ja ✓" & return
    set optionList to optionList & "• GPS-Ortsnamen: Ja ✓" & return

    -- Karten-Option
    if "$MAP_OPTION" is "Ja" then
        set optionList to optionList & "• Kartenausschnitt: Ja ✓" & return
        if "$REPROCESS_MAP" is "Ja, alle mit Karte" then
            set optionList to optionList & "• Bereits bearbeitete: Mit Karte versehen" & return
        end if
    else
        set optionList to optionList & "• Kartenausschnitt: Nein" & return
    end if

    if "$LOG_MODE" is "true" then
        set optionList to optionList & "• Modus: Erst Log anzeigen" & return
    else
        set optionList to optionList & "• Modus: Direkt umbenennen" & return
    end if

    set theChoice to button returned of (display dialog "📋 Zusammenfassung

📁 Ordner: $(basename "$folder_path")
📷 Fotos: $photo_count

Optionen:
" & optionList & "
Jetzt starten?" buttons {"Abbrechen", "Starten"} default button 2 with title "GPS Photo Renamer - Bestätigung")

    return theChoice
on error
    return "ABGEBROCHEN"
end try
APPLESCRIPT
)

if [[ "$confirm_result" != "Starten" ]]; then
    exit 0
fi

# Schritt 5: Verarbeitung starten
notify "Verarbeitung läuft" "Fotos werden umbenannt..."

# Python-Script ist im Resources-Ordner der App eingebettet
SCRIPT_DIR="$(dirname "$0")/../Resources"
PYTHON_SCRIPT="$SCRIPT_DIR/gps_photo_renamer_smart_counter.py"

if [ ! -f "$PYTHON_SCRIPT" ]; then
    show_error "Python-Script nicht gefunden!\n\nDie App scheint beschädigt zu sein."
    exit 1
fi

echo "Python-Script: $PYTHON_SCRIPT"
echo "Starte Verarbeitung..."

# Führe Python-Script aus und fange Output auf
OUTPUT=$(python3 "$PYTHON_SCRIPT" "$folder_path" $PYTHON_ARGS 2>&1)
EXIT_CODE=$?

echo "$OUTPUT"
echo ""
echo "Exit-Code: $EXIT_CODE"

# Extrahiere Statistiken aus dem Output
FOUND=$(echo "$OUTPUT" | grep -o "Found:[[:space:]]*[0-9]*" | grep -o "[0-9]*" | tail -1)
PROCESSED=$(echo "$OUTPUT" | grep -o "Processed:[[:space:]]*[0-9]*" | grep -o "[0-9]*" | tail -1)
SKIPPED=$(echo "$OUTPUT" | grep -o "Skipped:[[:space:]]*[0-9]*" | grep -o "[0-9]*" | tail -1)
VIDEOS=$(echo "$OUTPUT" | grep -o "Videos:[[:space:]]*[0-9]*" | grep -o "[0-9]*" | tail -1)
VIDEO_SIZE=$(echo "$OUTPUT" | grep -o "Video Size:[[:space:]]*[0-9.]*" | grep -o "[0-9.]*" | tail -1)

# Fallback-Werte
FOUND=${FOUND:-$photo_count}
PROCESSED=${PROCESSED:-0}
SKIPPED=${SKIPPED:-0}
VIDEOS=${VIDEOS:-0}
VIDEO_SIZE=${VIDEO_SIZE:-0}

# Schritt 6: Ergebnis anzeigen
if [ $EXIT_CODE -eq 0 ]; then
    if [[ "$LOG_MODE" == "true" ]]; then
        # Log-Modus: Frage ob jetzt umbenennen
        # Baue Video-Info für Dialog
        VIDEO_INFO=""
        if [ "$VIDEOS" -gt 0 ]; then
            VIDEO_INFO="
🎬 Videos gefunden: $VIDEOS (${VIDEO_SIZE} MB)"
        fi

        proceed_choice=$(osascript <<APPLESCRIPT 2>/dev/null
set theChoice to button returned of (display dialog "✅ Protokoll erstellt!

📊 Vorschau:
• Gefunden: $FOUND Fotos
• Würden umbenannt: $PROCESSED
• Würden übersprungen: $SKIPPED${VIDEO_INFO}

📄 Log: ~/Desktop/gps_photo_renamer.log

Jetzt die Fotos wirklich umbenennen?" buttons {"Abbrechen", "Log öffnen", "Jetzt umbenennen"} default button 3 with title "GPS Photo Renamer - Log fertig")
return theChoice
APPLESCRIPT
        )

        if [[ "$proceed_choice" == "Jetzt umbenennen" ]]; then
            notify "Umbenennen" "Fotos werden jetzt umbenannt..."

            # Führe ohne dry-run aus
            REAL_ARGS="${PYTHON_ARGS/--dry-run/}"
            OUTPUT=$(python3 "$PYTHON_SCRIPT" "$folder_path" $REAL_ARGS 2>&1)
            EXIT_CODE=$?
            echo "$OUTPUT"

            # Neue Statistiken extrahieren (inkl. Videos)
            PROCESSED=$(echo "$OUTPUT" | grep -o "Processed:[[:space:]]*[0-9]*" | grep -o "[0-9]*" | tail -1)
            VIDEOS=$(echo "$OUTPUT" | grep -o "Videos:[[:space:]]*[0-9]*" | grep -o "[0-9]*" | tail -1)
            VIDEO_SIZE=$(echo "$OUTPUT" | grep -o "Video Size:[[:space:]]*[0-9.]*" | grep -o "[0-9.]*" | tail -1)
            PROCESSED=${PROCESSED:-0}
            VIDEOS=${VIDEOS:-0}
            VIDEO_SIZE=${VIDEO_SIZE:-0}

            # Video-Info für Dialog
            VIDEO_INFO_LOG=""
            if [ "$VIDEOS" -gt 0 ]; then
                VIDEO_INFO_LOG="
🎬 Videos: $VIDEOS (${VIDEO_SIZE} MB)"
            fi

            if [ $EXIT_CODE -eq 0 ]; then
                osascript <<APPLESCRIPT
display dialog "✅ Fotos wurden umbenannt!

📊 Ergebnis:
• Umbenannt: $PROCESSED Fotos${VIDEO_INFO_LOG}

📁 Schema: YYYYMMDDHHMMSS_NNNN_Ort_Land.jpg

📄 Log: ~/Desktop/gps_photo_renamer.log" buttons {"OK", "Ordner öffnen"} default button 2 with title "GPS Photo Renamer - Fertig"

if button returned of result is "Ordner öffnen" then
    tell application "Finder"
        activate
        open POSIX file "$folder_path"
    end tell
end if
APPLESCRIPT
                notify "Fertig!" "$PROCESSED Fotos umbenannt"

                # Video-Löschung anbieten (nach Log-Modus umbenennen)
                if [ "$VIDEOS" -gt 0 ]; then
                    video_delete_choice=$(osascript <<APPLESCRIPT 2>/dev/null
try
    set theChoice to button returned of (display dialog "🎬 Videos gefunden!

Im Ordner wurden $VIDEOS Video-Dateien gefunden.
Gesamtgröße: ${VIDEO_SIZE} MB

Möchtest du diese Videos löschen?

⚠️ Dies kann nicht rückgängig gemacht werden!" buttons {"Behalten", "Videos löschen"} default button 1 with title "GPS Photo Renamer - Videos" with icon caution)
    return theChoice
on error
    return "Behalten"
end try
APPLESCRIPT
                    )

                    if [[ "$video_delete_choice" == "Videos löschen" ]]; then
                        echo "Lösche Videos..."
                        deleted_count=0
                        while IFS= read -r -d '' video_file; do
                            if [ -f "$video_file" ]; then
                                rm "$video_file"
                                echo "Gelöscht: $(basename "$video_file")"
                                ((deleted_count++))
                            fi
                        done < <(find "$folder_path" -maxdepth 1 -type f \( -iname "*.mp4" -o -iname "*.mov" -o -iname "*.avi" -o -iname "*.mkv" -o -iname "*.m4v" -o -iname "*.3gp" -o -iname "*.webm" -o -iname "*.insv" -o -iname "*.lrv" \) ! -name "._*" -print0)

                        notify "Videos gelöscht" "$deleted_count Videos wurden entfernt"
                        osascript -e "display dialog \"🗑️ Videos gelöscht!\n\n$deleted_count Video-Dateien wurden entfernt.\" buttons {\"OK\"} default button 1 with title \"GPS Photo Renamer\""
                    fi
                fi
            else
                osascript <<APPLESCRIPT
display dialog "❌ Ein Fehler ist aufgetreten beim Umbenennen!

📄 Log: ~/Desktop/gps_photo_renamer.log" buttons {"OK"} default button 1 with title "GPS Photo Renamer - Fehler" with icon stop
APPLESCRIPT
            fi
        elif [[ "$proceed_choice" == "Log öffnen" ]]; then
            open "$HOME/Desktop/gps_photo_renamer.log"
        fi
    else
        # Baue Video-Info für Dialog
        VIDEO_INFO_FINAL=""
        if [ "$VIDEOS" -gt 0 ]; then
            VIDEO_INFO_FINAL="
🎬 Videos: $VIDEOS (${VIDEO_SIZE} MB)"
        fi

        osascript <<APPLESCRIPT
display dialog "✅ Fotos wurden umbenannt!

📊 Ergebnis:
• Gefunden: $FOUND Fotos
• Umbenannt: $PROCESSED
• Übersprungen: $SKIPPED${VIDEO_INFO_FINAL}

📁 Die Fotos wurden nach dem Schema umbenannt:
   YYYYMMDDHHMMSS_NNNN_Ort_Land.jpg

📄 Log: ~/Desktop/gps_photo_renamer.log" buttons {"OK", "Ordner öffnen"} default button 2 with title "GPS Photo Renamer - Fertig"

if button returned of result is "Ordner öffnen" then
    tell application "Finder"
        activate
        open POSIX file "$folder_path"
    end tell
end if
APPLESCRIPT

        notify "Fertig!" "$PROCESSED Fotos verarbeitet"
    fi
else
    osascript <<APPLESCRIPT
display dialog "❌ Ein Fehler ist aufgetreten!

Bitte prüfe das Log für Details:
~/Desktop/gps_photo_renamer.log" buttons {"OK"} default button 1 with title "GPS Photo Renamer - Fehler" with icon stop
APPLESCRIPT
fi

# Schritt 7: Frage nach Video-Löschung (wenn Videos gefunden)
if [ "$VIDEOS" -gt 0 ] && [ "$EXIT_CODE" -eq 0 ] && [[ "$LOG_MODE" != "true" ]]; then
    echo ""
    echo "Videos gefunden: $VIDEOS (${VIDEO_SIZE} MB)"

    # Extrahiere Video-Dateinamen aus dem Output
    VIDEO_LIST=$(echo "$OUTPUT" | grep -A100 "🎬 Video files found:" | grep "^  •" | head -10)

    video_delete_choice=$(osascript <<APPLESCRIPT 2>/dev/null
try
    set theChoice to button returned of (display dialog "🎬 Videos gefunden!

Im Ordner wurden $VIDEOS Video-Dateien gefunden.
Gesamtgröße: ${VIDEO_SIZE} MB

Möchtest du diese Videos löschen?

⚠️ Dies kann nicht rückgängig gemacht werden!" buttons {"Behalten", "Videos löschen"} default button 1 with title "GPS Photo Renamer - Videos" with icon caution)
    return theChoice
on error
    return "Behalten"
end try
APPLESCRIPT
    )

    if [[ "$video_delete_choice" == "Videos löschen" ]]; then
        echo "Lösche Videos..."

        # Finde und lösche alle Videos im Ordner
        deleted_count=0
        while IFS= read -r -d '' video_file; do
            if [ -f "$video_file" ]; then
                rm "$video_file"
                echo "Gelöscht: $(basename "$video_file")"
                ((deleted_count++))
            fi
        done < <(find "$folder_path" -maxdepth 1 -type f \( -iname "*.mp4" -o -iname "*.mov" -o -iname "*.avi" -o -iname "*.mkv" -o -iname "*.m4v" -o -iname "*.3gp" -o -iname "*.webm" -o -iname "*.insv" -o -iname "*.lrv" \) ! -name "._*" -print0)

        notify "Videos gelöscht" "$deleted_count Videos wurden entfernt"

        osascript <<APPLESCRIPT
display dialog "🗑️ Videos gelöscht!

$deleted_count Video-Dateien wurden entfernt." buttons {"OK"} default button 1 with title "GPS Photo Renamer"
APPLESCRIPT
    else
        echo "Videos wurden behalten."
    fi
fi

echo ""
echo "=== FERTIG ==="
